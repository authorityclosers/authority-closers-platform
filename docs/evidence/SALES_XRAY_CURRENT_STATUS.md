# Sales Xray verified status — 13 September 2026

This supersedes earlier activation summaries in the individual slice evidence
files. Those files describe their earlier test scope, not the current release.

The additive provider follow-on is described in `SALES_XRAY_INFERENCE_WORKER.md`
and `SALES_XRAY_DURABLE_REPORTING.md`: internal C2 transcription, source-clock
support, durable C4 fact chunks, C5 coaching and C6 private drafts through a
separate bounded subprocess broker. It does not activate hosted intake or the
automatic upload-to-report UI pipeline.
Migration 0031 extends immutable history and provider-response erasure; the
frozen local integration and its browser receipts remain a separate slice.

`SALES_XRAY_HOSTED_AUTHORITY.md` adds opt-in source-owned API composition, finite
approved allowance/source-capacity admission and exact C2/C4/C5 quote/consent
routes with current worker rechecks. This supplies no live approval values,
provider free credits, worker deployment or automatic progression. Runtime,
provider-tested, staged and published status remain separate below.

## User-visible result

The supplied 20m53s recording has a private, source-checked AI sales draft outside
Git: `D:/AuthorityClosers-Private/SalesXray-Test/approved-call-20260913/Your-Sales-Call-Report.md`.
The adjacent HTML artifact contains the original recording, transcript and
timestamp controls. Browser policy rejected automatic navigation to that private
HTML file; its HTML structure was checked offline. It is not browser-verified.
The source recording, transcript, raw provider responses and report are not in
this repository. No report has been adjudicated by Dipak or measurement-validated
by Suyash. Numeric scoring remains withheld: actual weights 95, declared 100.

## Implemented

- Deterministic AudioAtlas execution and explicit SignalLab compatibility profiles,
  preserved native provenance, source-bound checkpoints and replay rules.
- Private storage and deletion, bounded local worker, AC tenancy/owner/session
  checks, immutable recording/quote/checkpoint lineage, PostgreSQL jobs/outbox,
  minute and budget reservation primitives.
- Local intake quote, separate exact consent, bounded source-byte transport and
  enqueue. No automatic entitlement grants or provider authorization.
- Separate AC-themed standalone client with choose-call, quote/consent,
  upload/progress and report presentation. Existing LMS remains intact.
- Bounded provider transports and separate private proof CLI; literal evidence
  validation, style-neutral fact extraction, source-backed Dipak draft profile,
  and private report export.
- Versioned provider/model/task registry and AC admin-only settings with external
  references, dormant adapter status and zero paid activation. Configurations are
  append-only; verified control-account identity and current admin permissions
  are checked on every read and save.
- Immutable local/test draft import, persisted transcript/report retrieval, saved
  history and authorized private audio range playback. A report is checked against
  retained original source bytes, native transcript hash, literal spans and profile.
  Draft deletion erases all three private JSON payloads while retaining audit hashes.
- Canonical provider-generated drafts, fact/checkpoint/profile reuse, transcript
  access before coaching, and proof-linked report reads. The internal durable
  path has synthetic PostgreSQL receipts; self-service activation remains pending.

The private proof CLI is not the production durable inference broker. The current
API run/report endpoint can return an imported private draft, or local analysis
status with `report: null`. Its local import accepts opaque operator-supplied
approval/generation/review receipt hashes; it does not independently authenticate
those external receipts or establish provider execution/human review. Hosted
composition rejects activation of this import path. Self-service new uploads still
run local measurements only; durable provider generation is not connected.

## Tested here

Receipts are under
`D:/Projects/authority-closers-release-transfer/2026-09-13-sales-xray/receipts/`.
Overlapping historical subset counts must not be added to these totals.

| Evidence | Observed result |
| --- | --- |
| `integrated-report-release.xml` | 377 passed, zero failed/skipped, 97.36s: conversation unit/contracts, composition and domain/worker/intake/admin/report/HTTP PostgreSQL integration |
| `http-regression.xml` | Earlier shared HTTP regression: 743 passed; not rerun after the final report/history route additions |
| `sales-xray-ui-vitest-v2.json` / `.xml` | 26 tests passed, zero failures; terminal polling, pending playback, history and run/recording/transcript/report binding |
| `integrated-ui-static-build-v2.log` | Updated standalone static build and TypeScript checks passed |
| Scoped Python Ruff / mypy | Passed; mypy checked 28 source files |
| `call-studio-browser.json` | Real browser/network UI flow with mocked API routes, zero provider calls |
| `browser/browser-network.json` | Earlier real HTTP synthetic example/capabilities and local workbench checks |
| `browser/real-source-local-playback.json` | Earlier source-matched local playback, no upload |
| `browser-authenticated-4.xml` | 8 passed: real TCP browser flow plus targeted report/HTTP PostgreSQL tests after the history response contract correction |
| `browser-authenticated-final.xml` and `browser-authenticated-final/authenticated-browser.json` | Final rebuilt UI: 1 browser test passed in 50.23s, eight recorded checks, zero observed browser errors/external browser requests; includes terminal polling verification |

Earlier browser upload tests use route mocks. The new real TCP browser/PostgreSQL
test uses real AC cookie authentication, server routes, private synthetic WAV
storage and a local native worker. It verifies reopening a prepared synthetic
report with source-bound transcript/audio, byte-range seeking, 390px reflow, reload,
and new file selection/quote/consent/upload/job completion. A completed local job
does not fabricate a generated sales report. The existing report is imported from
a synthetic provider-response fixture before browser execution; Google login,
provider inference and cross-subdomain sessions are outside this test scope.
Container isolation, production Node runtime, KVM4 processing capacity, canary
and rollback have not been verified by this lane.

## Provider-tested

The owner approved one exact recording and these providers at a zero paid-spend
cap. Credentials were injected from the external Infisical reference into the
bounded private worker. No keys are in this evidence or repository.

| Actual request | Result |
| --- | --- |
| ElevenLabs Scribe v2, original recording | HTTP 200; 144 segments, 19,806 transcript characters; native response preserved |
| Groq GPT-OSS 120B, fact chunks 1 and 2 | HTTP 200; 3,091 and 6,031 total tokens |
| Groq GPT-OSS 120B, initial coaching draft | HTTP 200; 5,868 total tokens; semantic quality failed |
| Groq GPT-OSS 120B, targeted correction | HTTP 200; 6,918 total tokens; semantic/literal quality failed |

The four real-call Groq requests used 21,908 total tokens. Correction reused the
original transcript; it did not retranscribe the audio. The coordinator checked
the report against source evidence after both generated coaching drafts failed.
All raw drafts remain preserved privately; none was promoted automatically.
The revised private report is a proposal for human review, not a successful
automated coaching-quality benchmark.

Fresh ElevenLabs subscription metadata showed the Free tier, 1,394 of 10,000
credits consumed and paid credit extensions disabled. No paid fallback or credit
purchase occurred. A reconciled provider invoice is not available; a zero spend
cap is not evidence of an independently verified invoice total.

Earlier synthetic Gemini generation returned 404 and 403. A successful model-list
request does not establish generation access. Sarvam generation was not tested.

## Staged / production-published / remaining work

**Staged: no. Production-published: no.** No Sales Xray public URL or VPS
deployment is claimed. The existing AC release is coordinated in the referenced
Release Recovery task; this lane has not changed its deployment or apex site.

Remaining work includes durable provider/checkpoint/report generation, current
hosted AC-authenticated browser/network proof, exact reviewer identity/feed mapping,
incremental review import, quality reproduction/calibration and approval,
hosted storage/retention composition, deployment target/release integration,
container and capacity tests, and canary/rollback receipts. Human adjudication and
production activation cannot be inferred from passing deterministic tests.
