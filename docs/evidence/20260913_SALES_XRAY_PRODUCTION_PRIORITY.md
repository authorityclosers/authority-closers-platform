# Sales Xray production priority — accepted founder direction, 2026-09-13

This is now the first release objective. Preserve the completed AC v0.2 consolidation, Admin/Coach/People corrections and licensed-video work; do not let unrelated cosmetic or cleanup work delay Sales Xray. The existing app goal remains active; this ledger supersedes its execution order and adds the acquisition funnel explicitly requested in the latest user message.

Ship a working, measured production Sales Xray on its reserved subdomain, with the same canonical Academy person and Cohorva username. Google is the prominent easy sign-in; email/password remains supported. A new Google sign-in creates the normal account and returns to the originating report without a separate email-verification journey when Google supplies a verified assertion. Preserve existing account-linking protections; do not manufacture verified identities.

Acquisition: ad/lead link -> focused centered audio dropzone -> clear recording consent/retention information -> upload and visible real processing progress -> useful visual report. Guests see roughly70% (overview, source-supported observations, key moments/audio clips and practical actions); deeper coaching/trends/history are unlocked by signing into the same Academy identity. Enforce the partition in API projections: never transmit locked details in HTML/JSON. Guest recordings and reports are private to an opaque server-issued guest session, not public URLs or IP ownership. No fake metrics or official autonomous score before SVAL. Unverified password registrants may continue their own guest preview; Google is the frictionless verified identity path. Login returns to the same report, and claiming a guest run must not reset its spent quota.

Collect name, email, phone and available canonical username progressively, with clear purpose and no duplicate account silo. Do not block the first upload behind a long signup form. Use an accessible username availability check against the existing public-profile service, preserving its rules and race-safe final claim.

Enforce100minutes server-side with measured audio duration, atomic reservation/settlement, per-person and guest allowance continuity, idempotent retries, provider spend/concurrency/rate limits and an Admin cutoff. IP/device/browser signals are secondary abuse signals, never identity or proof that a shared-network user is the same person. Prefer short-lived privacy-preserving signals and server-verified bot challenge; do not promise one-human-one-account certainty. Explain exhaustion with contact AC / request more access, not an unimplemented payment claim.

Report: coherent app shell, Overview / Call moments / Transcript / Coaching plan (and saved history/trends once real), exact timestamp seek and excerpts, intelligible English/Hinglish/Marlish labels as supported, plain-language evidence-specific actions from Dipak's controlled sales framework, visual physical channels with units/limitations. Never infer emotion, personality or sales success from pitch/tempo. Measure short-clip and whole-call latency; under a minute is a target to benchmark, not a fabricated SLA. Respect small viewports, keyboard, reduced motion and recovery.

Admin: existing named authorized admin@ and suyash@ have full operational access; Dipak has Coach plus explicitly granted review access. Same identities across apps, separate authorization. Persist multiple review submissions/lenses and reviewer assignment/invitation history in PostgreSQL, accessible to Codex through bounded reads. Queue shows authorized uploaded calls, current report and review state; notify only configured authorized recipients when analysis is ready, idempotently. No learner UI contains internal calibration/benchmarks.

Configuration: real provider/model dropdowns from validated supported model catalog; separate transcription and analysis selection; secret entry via the established secret-manager integration without echo/Git/logs; model-appropriate parameter ranges, versioned system/user prompt revisions, explicit draft/test/activate and rollback. Store every run's exact config/model/prompt versions, usage and cost estimate/actual reconciliation. Do not represent prompt tuning as provider weight training.

Analytics: per run, user, provider/model and total costs, durations, budget remaining, successes/failures/latency, queue/review state and real academy learner/activity summaries. Distinguish incurred billed usage from estimates and app logins from total registered users. Coach receives the explicitly authorized aggregate course/Sales metrics, not unrestricted personal data.

Benchmark budget authorized for intended initial funding: INR500 Gemini + INR500 ElevenLabs. No funding was observed yet. Reuse stored transcript/results, begin with small consented clips covering English/Hinglish/Marlish, clean/noisy, overlapping speech, discovery/objection/next-step; include a full20minute context case only when needed. Keep hard per-provider and total budget cutoffs. Confirm model availability and pricing from official sources; invented version examples are not configuration.

Completion: actual production URL and exact immutable source/image identity; normal Google + email account continuity; guest upload -> permitted preview -> same-person signup/claim -> unlocked report;100minute concurrency/idempotency/abuse tests; real configured provider analysis within spend cap; Admin reviewer save/reload and configurable notification; model/config test -> activation -> rollback; mobile report/audio; staging/prod isolated; no secrets or locked payload leaks. Record remaining limits honestly. Do not mark this objective complete just because UI builds or source merged.

Ownership: this root owns integration, guest/identity/quota boundary and single core deploy. Existing original Sales root owns report/engine/runtime/provider benchmarks; existing original UI root owns dedicated Admin reviewer admission, queue and model/control UX. No new subagents. All ready work merges to main/v0.2 only with validation; preserve dirty worktrees until integrated.

Integration preflight: the last reviewer CI stopped at formatting. The combined release corrects that test formatting and the final Sales browser/style test formatting before dispatch. Functional receipts remain in their source evidence; combined CI is still required.
