# Production parity integration

Observed at 2026-09-13T05:18:41.078550+00:00. This is source/integration evidence, not a deployment claim.

The isolated branch combines core41d3, reviewed package recovery9a21 and controller fix83dd, UI/social/Arcade/language changes through86bf141, canonical prerequisite CLI throughb475, Sales processing plans8c907, hosted runtime6205357, isolated credentials00edeb2/6b4b410, hosted servicef0303e9 and its required16k recipe correction4904d705. Registration main4c07669 is merged. Previous source f0303e9 alone is not an approved final hosted runtime.

All8baseline Arcade cards remain visible alongside3approved language sets. The3languages translate3semantic India scenarios; they are not9distinct cases. Formative practice retains separate authority from official scoring. Social discovery defaults private and requires opt-in; connections require acceptance. The source course commands preserve canonical audit and current permissions.

## Verification

Combined selected unit run:722passed,30failed,17skipped.29failures were test scratch directories under the user's home Git repository, correctly refused by private recording storage.1failure was the obsolete8-card HTTP expectation after adding the language sets. The test now verifies the exact8baseline+3new IDs and retains its origin/authentication checks.

Rechecking all30failures with fresh scratch outside every Git ancestor passed30/30. Combined distinct result:752passed,17platform/native skips. The16native numerical cases require the verified native build in CI; the remaining POSIX ownership case requires Linux. These skips are not claimed passed. Both original and recheck JUnit receipts are retained outside Git under2026-09-13-sales-xray/receipts/parity-integration-unit*.xml.

Linux-target mypy:251sourcefiles passed. Required Ruff lint/format on packages/python and tests passed after formatting imported changes. Prettier identified3newlychangedwebfiles and those were formatted. No unrelated script lint changes were made.

## Remaining before release

Integrate reviewed0032/0033backup/restore parity and populated migration proof, complete native-image packaging/activation inputs, then run the combined release CI. Publish only the images whose actual runtime/browser/media checks pass. Existing core41d3 deployment is independent and continues first.

## Integrated release verification update

The follow-on now includes the saved-report browser fix6483, native OCI/pool correction481f, recovery parity5521477/cfd0694 and socket normalizationa4f711. Actual native workflow review found no P0/P1; its Linux CI build remains required. Default-branch registration2c93ef5 is merged.

On6204dc4, the focused backup/restore/model-registry suite passed842 tests with17 explicit skips. The populated0032-to0033 PostgreSQL rehearsal passed1/1 selected, and actual migrated-head/model-registry comparison passed1/1 selected. Owned disposable databases were dropped and absence verified. Full nonsecret receipt: production-parity-6204dc4-20260913.json in the operator receipts directory. Linux mypy251files, all web Prettier checks, and required Python Ruff/format passed. On the final scanner-normalization integration, three selected socket tests passed.

Staging41d3 runtime became healthy after canonical source-owned installation with verified retained artifacts. The three source-course prerequisite commands returned prerequisites_committed for program189cec59-e302-4fe3-8215-a34c68e394a9. This is distinct from publication/media readiness; neither is claimed complete here. Production installation continues independently.

## Final combined candidate, 2026-09-13

Staging and production both run core 41d3c5726a1b5d217259c034bf058d9107135b1f with canonical migrations through0031 and runtime health verified. Production Resend recovery delivery to Dipak was observed in Gmail. Coach editing and the2GB chooser are live. Course publication and full4K playback are not yet complete: one staging upload intent expired with zero uploaded bytes after Windows curl framing failures; source fixes e99bacc7 and740464f preserve exact Content-Length and remove chunked-transfer conflict. Worker memory in the filesystem media overlay is now bounded at2GiB instead of512MiB, retaining1CPU, under eb1542a; application requires a new canonical release.

This candidate includes standalone Sales Xray auth/UI f1c5af0, strict profile origin installer c7c19ff, Docker store identity documentation59f4e4a, verified learner test-media disclosure3980508, and native/proc race regressions5697000. Report fixture changes from1bcb58f replace a contradictory test-only control-email monkeypatch with the actual verified control actor, retaining a direct non-control-admin denial regression; coherent PostgreSQL, HTTP and browser fixtures are integrated together. Provider/runtime admission policy is unchanged.

Source receipts: installer6new+18existing Bash-backed tests passed; report database6, HTTP1 and browser2 passed, plus the direct non-control-admin denial test1 passed. Native image6204dc4 actual isolated VPS Linux smoke passed twice and rejected the wrong image; its verified manifest is fd29cbf1edc9f6b13ca9fcebc6903adee0fd70583c77b60bd1753816f7f57ef9. No provider calls or real-call processing were performed. Temporary native and core workflow artifacts were removed only after retained image/archive and runtime verification. Final integrated CI remains required; these facts are not a final-candidate deployment claim.
