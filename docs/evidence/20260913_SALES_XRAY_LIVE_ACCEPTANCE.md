# Sales Xray live acceptance continuation

The owner requires staging upload-to-report acceptance, then the same accepted
artifact on production with another actual upload-to-report test. A reachable
frontend does not satisfy that outcome. The September13 clarification also makes
report exploration, prominent supported measurements, source-linked audio moments,
export, onboarding, accessible motion and language controls part of acceptance.
Provider/model administration remains restricted to the verified control admin.
Preserve the95actual/100declared discrepancy and hold numeric coaching publication.

## Observed baseline

At2026-09-13T15:46Z, both core environments still referenced
69db2257b2671baa3aefc7902e69078f381a649d. Actual Chrome navigation to
`https://learner-staging.authorityclosers.com/sales-xray` showed a connected AC
account, upload disabled for that workspace, and no saved calls. It also exposed
two identical Sales Xray entries in the desktop navigation. No hosted report
completion or production upload has been observed in this continuation.

The navigation correction removes the duplicate while retaining the canonical
route, active state and audio icon. The mounted shell regression now checks that
the learner navigation contains exactly one active Sales Xray destination.
Four focused shell/practice tests passed; JUnit:
`D:/AC-authority-closers-release-audit/learner-xray-nav-deduplicate.xml`.
Prettier and targeted ESLint passed. These local commands used Node22.17.0;
the repository requires Node24, so this is not a replacement for release CI.

## Release and external evidence

Candidate99cdee5e19e298173e4bb8e8619087ef70cf9ccf passed the full Linux
validation job in run34765395716:5088Pythonpasses,786skips, zero failures.
The required separate root activation job passed33tests with zero skips.
The immutable application image packaging job was still running when this
record was written. The successful standalone build34765409663 is bound to
that frozen source, despite the dispatch workflow's separate main-branch head.
Its exact archive/OCI verification receipt is
`D:/AC-authority-closers-release-audit/web-99cdee5-receipt.txt`.

Only already verified and preserved Actions transfer copies10313953565 and
10320696967 were rotated to admit subsequent artifacts under the existing450MB
pool ceiling. Their local copies, hashes, CI records and rotation receipts remain
under `D:/AC-authority-closers-release-audit`. No storage purchase was made.

The source-owned routing controller is dry-run by default and acts on only the
selected environment. It verifies exact account/zone/tunnel, preserves complete
tunnel configuration, refuses pre-write drift, verifies readback before DNS
creation, and refuses conflicting records. Only the exact Cloudflare API origin
receives live credentials; the loopback test server requires a fixed test token.
Configuration fingerprints sort object keys while preserving array order.
Sixteen mocked/offline tests passed with zero skips; no Cloudflare mutation:
`D:/AC-authority-closers-release-audit/salesxray-routing-junit-20260913.xml`.

Fresh browser observations show ElevenLabsFree with8606credits remaining and
GroqFree with inference ZDR enabled, GlobalZDR disabled. The observations are
evidence, not a new recording authorization. No key was opened, settings changed,
credit purchased or provider inference performed in this continuation.

## Outstanding acceptance

| Outcome | Status at this receipt |
| --- | --- |
| Full candidate Linux source validation | Pass for99cdee5 |
| Standalone image build, HTTP and immutable identity | Pass for99cdee5 |
| Core immutable image publication | Running |
| Improved report UX and supported language controls | In implementation |
| VPS DB-only worker credential provisioner | In implementation |
| Exact staging/production approval bundles | Pending |
| Staging upload, progress, report, replay, deletion and IDOR | Not run |
| Production canary, upload-to-report and rollback evidence | Not run |
| Public standalone host publication | Not performed |

The previous one-run approval was for local-only outputs. A new explicit question
requests one staging and one production run of the attached20m53sWhatsApp source,
ElevenLabsScribev2 plus GroqGPT-OSS120B, zero cost with no paid fallback, private
VPS storage for one day followed by deletion. It remains pending at this receipt.
Do not infer an approval from this document or the older proposal JSON.
