#!/usr/bin/env bash
set -euo pipefail

message_id="${1:?Pass the Resend message id returned by ac-resend-send-test}"
exec /usr/local/sbin/ac-infisical-run \
  -- /usr/local/sbin/ac-resend-delivery-check-inner "$message_id"
