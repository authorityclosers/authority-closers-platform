# Selected only by this target release's reviewed staging capability policy.
# Never include this file directly for production or via an ambient env flag.
services:
  api:
    environment:
      AC_MEDIA_PROVIDER_ENABLED: "false"
      AC_MEDIA_STRESS_FIXTURES_ENABLED: "true"
      AC_MEDIA_STRESS_FIXTURES_CACHE_ROOT: /run/ac-staging-public-films
      AC_MEDIA_STAGING_PUBLIC_FILMS_DELIVERY_ENABLED: "true"
    volumes:
      - type: bind
        source: /srv/authority-closers/application/media-staging/d693acc73dc3f66c1b13ad6e68d5e41cba8478dc719f4b68211ce829442b0222
        target: /run/ac-staging-public-films
        read_only: true
        bind:
          create_host_path: false
