# Infrastructure

VPS target:
`/srv/authority-closers/{compose,env,volumes,backups,runbooks,releases}`

Git/release artifacts are authoritative for deployable configuration.
Secrets are excluded.
First external cohort requires demonstrated staging restore and side-effect reconciliation.
